import { ClickoutsideDropdownDirective } from './clickoutside-dropdown.directive';

describe('ClickoutsideDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickoutsideDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
